вопросы

1 как знучшм ентерференс обрабатывает поля
2 он вызываетса для каждошо поля
3 как отрефакторить чтобы за один вызов он обрабатывал все поля и интегрировать в фраймворк при етом максимум декларативная природа
4 мне кажытса что для инференс евентов генерируетса слишком много связей
===
5 как работает поиск в приложении
 Сейчас поиск в explorer.py реализован как заглушка: self._mapper.query(Asset).all(session). Он просто возвращает все ассеты из базы, игнорируя текст запроса.
===
6 Что возвращает при пустом поле: При пустом поле он также возвращает все ассеты. Если в поле что-то введено, результат не меняется, так как фильтрация еще не реализована в коде search().
===
7 реализованы ли бачи процесинга и ингестион на уровне модуля